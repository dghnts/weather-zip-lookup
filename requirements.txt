# Core dependencies (required for production)
requests>=2.31.0
flask>=3.0.0

# CLI dependencies (optional for local use)
colorama>=0.4.6

# Testing dependencies (development only)
pytest>=7.4.0
hypothesis>=6.92.0
responses>=0.24.0
playwright>=1.40.0
pytest-playwright>=0.4.3
